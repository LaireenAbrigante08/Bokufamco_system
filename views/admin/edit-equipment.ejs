<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Equipment</title>
    <style>
        /* Basic Styles for the Form */
       
        h1 {
            text-align: center;
            margin-bottom: 20px;
            color: #006400;
        }
        .form-container {
            max-width: 500px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }
        .form-container label {
            margin-bottom: 8px;
            display: block;
            font-weight: bold;
        }
        .form-container input, .form-container textarea, .form-container button, .form-container select {
            width: 100%;
            padding: 10px;
            margin: 8px 0;
            font-size: 16px;
            border: 1px solid #ddd;
            border-radius: 4px;
            box-sizing: border-box;
        }
        .form-container textarea {
            resize: vertical;
        }
        .form-container button {
            background-color: #4CAF50;
            color: white;
            border: none;
            cursor: pointer;
            font-weight: bold;
        }
        .form-container button:hover {
            background-color: #45a049;
        }
        .back-link {
            display: block;
            text-align: center;
            margin-top: 15px;
            color: #006400;
            text-decoration: none;
            font-weight: bold;
        }
        .back-link:hover {
            text-decoration: underline;
        }
        .current-picture {
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <h1>Edit Equipment</h1>
    <div class="form-container">
        <form action="/admin/equipment/edit/<%= equipment.id %>" method="POST" enctype="multipart/form-data">
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="<%= equipment.name %>" required>

            <label for="description">Description:</label>
            <textarea id="description" name="description" rows="4" required><%= equipment.description %></textarea>

            <label for="price">Price:</label>
            <input type="number" id="price" name="price" value="<%= equipment.price %>" required>

            <label for="stockQuantity">Stock Quantity:</label>
            <input type="number" id="stockQuantity" name="stockQuantity" value="<%= equipment.stock_quantity %>" required>

            <label for="status">Status:</label>
            <select id="status" name="status" required>
                <option value="Available" <%= equipment.status === 'Available' ? 'selected' : '' %>>Available</option>
                <option value="Rented" <%= equipment.status === 'Rented' ? 'selected' : '' %>>Rented</option>
                <option value="Maintenance" <%= equipment.status === 'Maintenance' ? 'selected' : '' %>>Maintenance</option>
            </select>

            <label for="currentPicture">Current Picture:</label>
            <div class="current-picture">
                <img src="/images/product/<%= equipment.picture %>" alt="<%= equipment.name %>" style="width: 100px;">
            </div>

            <label for="picture">Change Picture:</label>
            <input type="file" id="picture" name="picture" accept="image/*">

            <input type="hidden" name="currentPicture" value="<%= equipment.picture %>">

            <button type="submit">Update Equipment</button>
        </form>
    </div>
    <a class="back-link" href="/admin/equipment">Back to Equipment List</a>
</body>
</html>
