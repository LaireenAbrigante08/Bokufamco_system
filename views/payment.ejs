<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Loan Payment</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <div class="container mt-5">
        <h2>Loan Payment</h2>
        
        <!-- Loan Details -->
        <div class="card mb-4">
            <div class="card-body">
                <h5 class="card-title">Loan Details</h5>
                <p><strong>Loan Type:</strong> <%= loan.loan_type %></p>
                <p><strong>Loan Amount:</strong> ₱<%= loan.loan_amount.toFixed(2) %></p>
                <p><strong>Interest Rate:</strong> <%= loan.interest_rate %>%</p>
                <p><strong>Total Repayment:</strong> ₱<%= loan.total_repayment.toFixed(2) %></p>
                <p><strong>Due Date:</strong> <%= loan.due_date %></p>
            </div>
        </div>

        <!-- Payment Form -->
        <form action="/loans/pay/<%= loan.id %>" method="POST">
            <div class="mb-3">
                <label for="paymentAmount" class="form-label">Enter Payment Amount</label>
                <input type="number" step="0.01" name="paymentAmount" class="form-control" required>
            </div>
            <button type="submit" class="btn btn-primary">Pay</button>
        </form>
        
        <br>
        <a href="/loans" class="btn btn-secondary">Back to Loan List</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
